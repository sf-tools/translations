{
    "name": "SF Tools",
    "pages": {
        "query": {
            "buttons":{
                "clear": {
                    "label": "Clear",
                    "description": "Clear the results table"
                },
                "execute": {
                    "label": "Execute Query",
                    "description": "Executes the query from the editor and displays the results in a table"
                },
                "stop": {
                    "label": "Stop",
                    "description": "Stop the current query execution"
                },
                "export": {
                    "label": "Export as {{format}}",
                    "description": "Download the exported data as the selected format"
                }
            }
        }
    },
    "navigation": {
        "tabs": {
            "home": "Home",
            "permissions": "Permissions",
            "query": "Query",
            "logs": "Logs",
            "metadata_explorer": "Metadata Explorer",
            "settings": "Settings"
        }
    },
    "globals": {
        "file": {
            "format": {
                "json": "JSON",
                "csv": "CSV",
                "xml": "XML"
            }
        }
    }
}
